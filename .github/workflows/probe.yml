name: probe

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: probe
  cancel-in-progress: false

jobs:
  watchdog:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # Ölçüm noktasının son sonucu (tek commit'lik `heartbeat` dalı).
      - name: Nabzı al
        run: |
          git fetch --depth=1 origin heartbeat && git show FETCH_HEAD:heartbeat.json > heartbeat.json || echo "{}" > heartbeat.json

      - name: Değerlendir
        env:
          KNOWN_ISSUES: ${{ vars.KNOWN_ISSUES }}
        run: python watchdog.py

      # Durum değiştiyse (ya da günde bir kez, deponun etkin kalması için) kaydet.
      - name: Kaydet
        if: always()
        run: |
          hour=$(date -u +%H); minute=$(date -u +%M)
          if [ -f .changed ] || { [ "$hour" = "03" ] && [ "$minute" -lt 10 ]; }; then
            git config user.name "beamvio-status"
            git config user.email "actions@users.noreply.github.com"
            git add docs/status.json docs/incidents.json
            git diff --cached --quiet || git commit -m "durum: $(date -u +%Y-%m-%dT%H:%MZ)"
            git push
          fi
